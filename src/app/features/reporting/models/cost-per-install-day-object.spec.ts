import { CostPerInstallDayObject } from './cost-per-install-day-object';

describe('CostPerInstallDayObject', () => {
  it('should create an instance', () => {
    expect(new CostPerInstallDayObject()).toBeTruthy();
  });
});
